{
  "name": "pump-cycle",
  "version": "0.0.1",
  "scripts": {
    "build": "mkdir -p dist && ./node_modules/babel/bin/babel.js --stage 1 lib --out-dir dist",
    "test": "./node_modules/mocha/bin/mocha --compilers js:babel/register -t 10000 -R list"
  },
  "dependencies": {
    "chalk": "^1.1.1",
    "lodash": "^3.10.1",
    "rx": "^3.1.1"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "mocha": "^2.2.5"
  }
}
